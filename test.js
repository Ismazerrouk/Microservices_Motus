const express = require('express');
const app = express();
const port = 3000;

app.get('/', (req, res) => {
  res.send('Hello, World!');
});

app.listen(port, () => {
  console.log(`Le serveur est en cours d'exécution à [1](http://localhost):${port}`);
});
